thomass/gb
==========

Executes golangs gb.

usage
-----

cd to your working directory and execute:

```
sudo docker run --rm -v "$PWD":/src thomass/gb
```

info
----

* ENTRYPOINT is _gb_
* CMD is _build_
